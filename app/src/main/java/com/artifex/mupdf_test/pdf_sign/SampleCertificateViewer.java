package com.artifex.mupdf_test.pdf_sign;

import android.os.Bundle;

import com.artifex.sonui.editor.NUICertificateViewer;

public class SampleCertificateViewer extends NUICertificateViewer {

    // Present a Certificate Viewer dialog, similar to the SampleCertificatePicker
    // dialog, in order to show details of the certificate sed for signing a PDF.
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
    }
}
